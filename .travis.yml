dist: xenial

language: python
python:
  - "3.7"

addons:
  apt:
    packages:
      - g++
      - freeglut3-dev

install: pip3 install cmake==3.13.1
script:
  - cmake --version
  - cmake -Bbuild .
  - cmake --build build
